{
  "name": "ephemeral-pages-action",
  "version": "1.0.1",
  "description": "Publish temporary HTML reports from same-repository pull requests to Ephemeral Pages.",
  "keywords": [
    "github-action",
    "html-report",
    "pull-request"
  ],
  "license": "MIT",
  "author": "Schalk Neethling",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/schalkneethling/ephemeral-pages-action.git"
  },
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts --minify --source-map",
    "bundle:check": "pnpm build && git diff --exit-code -- dist/index.js dist/index.js.map dist/package.json dist/sourcemap-register.cjs",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint --config oxlint.json src scripts tests",
    "release": "node scripts/dispatch-release.ts",
    "release:check": "pnpm quality && node scripts/release-check.ts && node scripts/repository-controls.ts",
    "repo:controls:apply": "node scripts/repository-controls.ts --apply",
    "repo:controls:check": "node scripts/repository-controls.ts",
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit && tsc --project tsconfig.scripts.json",
    "quality": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm bundle:check"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@vercel/ncc": "^0.44.1",
    "@vitest/coverage-v8": "^3.2.4",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.10.0"
}
